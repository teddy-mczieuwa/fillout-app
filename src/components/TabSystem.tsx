'use client';

// Re-export the modular TabSystem component
import ModularTabSystem from '@/components/tabs/TabSystem';
export type { Tab } from '@/hooks/useDraggableTabs';

// Export the modular version as the default export
export default ModularTabSystem;
